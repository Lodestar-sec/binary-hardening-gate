apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "app.fullname" . }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
# The app does not call the Kubernetes API; no token is mounted.
automountServiceAccountToken: false
